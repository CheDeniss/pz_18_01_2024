import React from 'react';

const Placeholder404 = ({id}) => {
    return (
        <div>
            <h1>Дані не знайдені.</h1>
            <h2>id: {id}</h2>
        </div>
    );
};

export default Placeholder404;
